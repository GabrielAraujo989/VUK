{
  "name": "vuk",
  "version": "1.0.0",
  "scripts": {
    "start": "cd frontend && npm start",
    "build": "cd frontend && npm install && NODE_OPTIONS=--max-old-space-size=460 npm run build"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "http-server": "^14.1.1"
  }
}
